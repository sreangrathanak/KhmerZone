<% if user_signed_in? %>
  <div class="row home-info">
      <div class="row">
        <div class="col-md-9">
          <%= render "shared/feed" %>
        </div>
        <div class="col-md-3">
          Suggested Shop
        </div>
      </div>
  </div>
<% else %>
<% end %>