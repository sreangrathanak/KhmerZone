<% unless current_sign_in_user?(@user) %>
  <div id="follow_form-<%=@user.id%>">
  <% if current_user.following?(@user) %>
    <%= render "users/unfollow" %>
  <% else %>
    <%= render "users/follow" %>
  <% end %>
  </div>
<% end %>