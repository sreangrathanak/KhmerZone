<%= form_for(current_user.product_likes.build,remote: true) do |f| %>
  <div><%= hidden_field_tag :product_id, @product.id %></div>  
  <%= button_tag(type: 'submit', class: "") do %>
    <span class="glyphicon glyphicon-thumbs-up btn_like" aria-hidden="true"></span>
  <% end %>  
<% end %>
<%if @product.product_likes.count > 0%>    
    <%="#{@product.product_likes.count}" if @product.product_likes.count > 0 %>
    like this.
<%end%>